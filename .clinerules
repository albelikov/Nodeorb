# Правила для Cline — Nodeorb Logistics Platform

## 🚫 СТРОГО ЗАПРЕЩЕНО

### Файлы конфигурации (НИКОГДА не редактировать)
- `gradle/libs.versions.toml` — централизованные версии зависимостей
- `settings.gradle.kts` — структура монорепозитория
- `build.gradle.kts` (в корне и сервисах) — конфигурация Gradle
- `gradle.properties` — глобальные свойства проекта
- `package-lock.json` (во всех фронтендах) — блокировка версий npm/yarn
- `.env`, `.env.example`, `.env.*`, `local.properties` — секреты и окружение
- `Dockerfile*`, `docker-compose*.yml` — без явного запроса
- Лицензионные файлы (`LICENSE`, `NOTICE`)

### Директории (только чтение)
- `build/` — сгенерированные файлы (во всех сервисах)
- `node_modules/` — зависимости npm
- `.gradle/`, `.kotlin/`, `.idea/` — кэш и метаданные инструментов
- `gradle/wrapper/` — обёртка Gradle

### Действия
- Изменять версии зависимостей в `libs.versions.toml`
- Удалять/изменять публичные API в `api/` директориях (например, `scm-service/api/`)
- Менять сигнатуры методов в `internal/` без согласования
- Удалять миграции БД (`scm-service/migrations/`)
- Изменять структуру монорепо (добавлять/удалять модули в `settings.gradle.kts`)

## ✅ РАЗРЕШЕНО РЕДАКТИРОВАТЬ

### Бэкенд (Kotlin)
- `*/src/main/kotlin/` — бизнес-логика сервисов
- `*/src/main/resources/` — конфигурации (без секретов)
- `*/src/test/kotlin/` — тесты (только добавление/исправление)

### Фронтенд
- `frontend/site/src/` — статический сайт (React/TypeScript)
- `frontend/user-portal/src/` — пользовательский портал
- `admin-frontend/src/` — админка (Kotlin Multiplatform)

### Документация
- `*/README.md` — только разделы с документацией (не метаданные)
- `build-summary.md`, `build-progress.md` — статус сборки

## 📌 ТРЕБОВАНИЯ К ИЗМЕНЕНИЯМ

1. **Сохранять обратную совместимость** — не ломать существующие API
2. **Не удалять комментарии и JSDoc/KDoc** без причины
3. **Следовать существующему стилю кода** (Kotlin: ktlint, TypeScript: ESLint/Prettier)
4. **Все изменения требуют моего подтверждения** — автоподтверждение отключено для:
   - `Edit files`
   - `Run commands`
5. **После изменений** — запускать соответствующие тесты:
   - Бэкенд: `./gradlew test`
   - Фронтенд: `npm test` в соответствующей директории

## ⚠️ ОСОБОЕ ВНИМАНИЕ

Это **логистическая платформа с микросервисной архитектурой**. Любое изменение в одном сервисе может повлиять на другие. Перед изменением:
1. Проверить зависимости между сервисами
2. Убедиться, что не нарушена контрактная совместимость
3. Протестировать интеграционные сценарии