# Finance-Legal Service Configuration
server {
    port = 50055
}

database {
    url = "jdbc:postgresql://localhost:5432/nodeorb"
    user = "postgres"
    password = "postgres"
    driver = "org.postgresql.Driver"
}

kafka {
    bootstrap.servers = "localhost:9092"
}

grpc {
    server {
        port = 50055
    }
}

// Customs Service integration
customs {
    host = "localhost"
    port = 50056
}

// Currency conversion API
currency {
    api {
        url = "https://api.exchangerate-api.com/v4/latest"
        key = ""
    }
}

# Payment gateway configuration
payment {
    stripe {
        secret.key = ""
    }
    paypal {
        client.id = ""
        client.secret = ""
    }
}

# Tax service integration
tax {
    api {
        url = "https://api.tax.service.gov.uk"
        key = ""
    }
}

# Customs integration
customs {
    api {
        url = "http://localhost:8080"
    }
}